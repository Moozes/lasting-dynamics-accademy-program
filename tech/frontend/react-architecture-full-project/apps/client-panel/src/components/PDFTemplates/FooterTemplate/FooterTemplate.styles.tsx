import { StyleSheet } from "@react-pdf/renderer";

export const styles = StyleSheet.create({
    footer: {
        position: "absolute",
        bottom: 10,
        textAlign: "center",
        width: "100%",
        fontSize: 7,
    },
});
